# Pinned per fintech_coding_guidelines.md §5. Deliberately minimal.
# Later milestones add their own pins when they start (lightgbm, cvxpy, ...).
pandas==2.3.1
pyarrow==20.0.0
requests==2.32.4
python-dotenv==1.1.0
pytest==8.4.1

# M2 — daily equity bars
yfinance==1.4.1
exchange_calendars==4.13.2

# M3 — baseline prediction pipeline
lightgbm==4.6.0
scikit-learn==1.9.0  # LGBMRegressor's sklearn API needs it; also LedoitWolf (M4)

# M4 — portfolio controller (constrained mean-variance)
cvxpy==1.9.2

# M6 — paper trading (Alpaca)
alpaca-py==0.43.4

# M9 — risk overlay (GARCH conditional-vol forecast for vol-targeting)
arch==7.2.0
